export type Device = {
    device_id: number,
    device_name: number,
    wallet_address: string,
    device_ip?: string,
    device_location?: string,
    device_type?: number,
    device_version?: number,
}